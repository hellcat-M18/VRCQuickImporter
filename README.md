# VRCQuickImporter

BOOTHで購入したアセットをUnity Editor内から管理できるVRChat向けエディタ拡張ツールです。

BOOTH: [VRCQuickImporter](https://hellcat.booth.pm/items/8616786)

> **本ツールはBOOTHの非公式ツールです。**
> 公式の提供物ではなく、利用は自己責任です。
> 過剰なリクエスト・アクセスが発生しないよう設計してはいますが、予めBOOTHの[利用規約](https://policies.pixiv.net/)・[ガイドライン](https://booth.pm/guidelines)を念頭に置き、常識的な範囲でご利用ください。

---

## 導入方法

### 動作要件

- Windows
- Unity 2022.3以降（推奨: 2022.3.22f1）
- WebView2 Evergreen Runtime（未導入時は自動インストール）

### BOOTHで購入（300円）

[BOOTH](https://hellcat.booth.pm/items/8616786)で購入後、ダウンロードした`.unitypackage`をダブルクリックして導入してください。

※GitHubでも無料公開しています。**製作者を応援したい方はBOOTHからどうぞ。**

### GitHubから（無料）

1. Releasesから導入したいバージョンを選択し、zipファイルをダウンロード
2. ダウンロードしたzipファイルを展開し、UnityプロジェクトのAssets直下に配置

### 初回セットアップ

1. Unityメニュー `Tools > VRCQuickImporter` でウィンドウを開きます。
2. 「詳細設定 / トラブルシュート」を開き、「BOOTHログイン画面を開く」からWebView2で開いたBOOTH公式画面へログインします。
   - 本ツールはBOOTHのパスワードを保存・送信しません。認証はBOOTH公式ページ上で行われます。
3. 画面上部の「初回セットアップ」を押すと、確認ダイアログが表示されます。同意するとライブラリ全ページの取得を開始します。
4. 同期完了後、カードグリッドに購入済み商品が表示されます。

以降は原則としてBOOTHへの再ログインが不要になります。セッション情報はプロジェクト内の専用プロファイルに保存されます。

---

## 通常の使い方

### 購入履歴をBoothと同期する

「BOOTHと同期」を押すと、前回の同期以降の新しい商品データを追加取得します。既存の商品と重複したか、新しいページが空になった時点で自動停止します。

### 商品を探す

カード一覧からスクロールして探すか、画面上部の検索欄に商品名の一部を入力して絞り込みます。検索はローカルキャッシュに対して行われ、BOOTHへのアクセスは発生しません。

### ダウンロード

商品カードのダウンロードボタンを押すと、helperプロセス経由でファイルを`downloads/`に保存します。進捗バーが表示されます。

ダウンロードボタンを押すと、自動でBOOTH上の最新情報を確認します。

- ダウンロードURLが変更されていた場合は自動更新して続行
- ファイル構成が変わっていた場合はダイアログで通知

### インポート

「ダウンロード＆インポート」を押すと、ダウンロード完了後にファイル形式に応じてUnityへの取り込みを開始します。

- `.unitypackage`が1つだけ → 自動インポート
- `.unitypackage`が複数 → どのファイルをインポートするか確認
- `.unitypackage`なし → `Assets/BOOTH/{zip名}/` に展開

### フォルダを探す

商品カードをダブルクリックすると、インポート済みのファイルがProjectウィンドウでハイライト表示されます。
複数回インポートしている場合は、どのフォルダを開くか選択できます。

> このツールでインポートしたものだけが追跡対象です。手動で配置したファイルや他のツール経由でインポートしたファイルは表示されません。

---

## オプション機能

通常の利用では使わない機能です。詳細設定/トラブルシュートセクションにまとめられています。

### 同期キャンセル

同期中に「キャンセル」ボタンを押すと、取得途中のhelperプロセスを終了します。既存のキャッシュデータは保護されます。

### 完全リフレッシュ

ライブラリデータのキャッシュを一旦破棄し、BOOTHライブラリを全件再取得します。詳細設定内の「同期のメンテナンス」から実行できます。
アクセス集中を回避するため、実行には数分ほどかかります。

完全リフレッシュ中は商品カードがクリアされ、ウィンドウ内に進行バーが表示されます。
何ページ目を取得中か、累計で何商品かを確認できます。

### データ削除/リセット

詳細設定から個別に削除できるのはWebView2ログイン情報/プロファイルです。

ライブラリキャッシュ、サムネイル、ダウンロード済みファイル、展開済みファイル、ログは、データフォルダを開いて手動で削除できます。
`Library/VRCQuickImporter/` 全体を削除しても再生成されますが、WebView2プロファイルも消えるため再ログインが必要になります。

### ログイン再認証

セッションが切れた場合や、別のBOOTHアカウントに切り替える場合は、詳細設定からWebView2プロファイルを削除して再ログインします。

---

## 技術詳細

### アーキテクチャ

本ツールはUnity Editor拡張と、別プロセスのWebView2 helperで構成されています。

WebView2をUnityプロセス内に組み込むと、描画スレッドの干渉によりUnity本体がクラッシュするリスクがあります。このため、helper exeとして独立させ、Unity EditorからはJSONファイル経由で通信する設計としています。

```text
Unity Editor (UI / Import / DB管理)
    ↓ JSON経由
WebView2 Helper (BOOTHログイン / ライブラリ同期 / ダウンロード)
    ↓ HTTP/HTML
BOOTH
```

### BOOTHアクセスの設計

ライブラリページのHTMLをDOM解析し、商品名・ショップ名・ファイル名・ダウンロードURL等を抽出します。
抽出にはCSSクラス名ではなく、商品URLやダウンロードボタンのセマンティックな属性（`data-test`等）を使用しており、BOOTHのデザイン変更にある程度耐性があります。

アクセス制御:

- BOOTHライブラリページへのアクセスは**最低5秒の間隔**を空けます
- 並列アクセス・確認無しの自動アクセスは行いません
- 同期時は画像・メディア・フォントリソースをブロックし（204 No Content）、BOOTHサーバへの負荷を抑えます。JavaScript/CSSはブロックしません
- 増分同期では、内容が既存キャッシュと被った時点で取得を停止します
- 完全リフレッシュはユーザーが明示的に実行したときにのみ行われます

### セッション管理

BOOTHのログインは、本ツール専用のWebView2プロファイル上で行われます。

- Chrome/Edgeなど既存ブラウザのCookieやログインセッションは流用しません
- BOOTHのパスワードを本ツールが読み取ることはなく、認証はBOOTH公式ページ上で完結します
- セッション情報はプロジェクト内の`Library/VRCQuickImporter/webview-profile/`にのみ保存されます

### データ管理

取得したライブラリデータは`database.json`としてプロジェクト内にキャッシュされます。

- 書き込み時は`.tmp`ファイル経由で原子的に置き換え、既存ファイルを`.bak`に退避します
- 同期キャンセルや失敗時は、取得途中のデータを破棄し既存キャッシュを保護します

---

## データ保存先

実行時データはプロジェクト内の`Library/VRCQuickImporter/`以下に保存されます。

```text
Library/VRCQuickImporter/
  cache/                      各種キャッシュ
  thumbnails/                 サムネイル画像キャッシュ
  downloads/                  ダウンロード済みZIP/ファイル
  extracted/                  展開済みファイル
  logs/                       同期/ダウンロードログ
  webview-profile/            WebView2ログイン用プロファイル
  database.json               同期済みライブラリ
  database.json.bak           database.jsonのバックアップ
  import-history.json         このツール経由でインポートしたアセットの履歴
  pending-page.json           取得途中の1ページ分（同期完了後に削除）
```

`Library/VRCQuickImporter/` を削除しても再生成されますが、WebView2プロファイルも消えるため再ログインが必要になります。

---

## 既知の制限

- BOOTHのページ構造やダウンロードURL形式が大幅に変更されると、一時的に同期が動作しなくなる可能性があります（v0.2.1でセマンティック抽出に移行し、デザイン変更耐性を向上）
- Windows専用です。macOS/Linuxには対応していません
- 商品のタグやカテゴリの取得は行っていません（取得には商品詳細ページへの大量アクセスが必要なため）

---

## 免責事項

- 本ツールはBOOTH公式ツールではありません
- 利用は自己責任です
- BOOTH側の仕様変更や判断により、利用が制限される場合があります。その場合は速やかに使用を停止してください
- ログインはBOOTH公式ページ上で行われます。本ツールはパスワードを保存・送信しません
- セッション情報はプロジェクト内のWebView2専用プロファイルに保存されます。他のブラウザのCookieを読み取りません
- 取得・保存するのはログインユーザー自身の購入履歴情報のみです。第三者への提供や再配布は行いません
